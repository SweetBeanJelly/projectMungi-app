<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finedust.abettertomorrow">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main" />
        <activity android:name=".TodayWeeksAdapter" />
        <activity android:name=".CommunityAdapter" />
        <activity android:name=".PageCalendar" />
        <activity android:name=".PageAlarm" />
        <activity android:name=".PageTimetable" />
        <activity android:name=".PageToday" />
        <activity android:name=".PageWeeks" />
        <activity android:name=".list_calendar_item" />
        <activity android:name=".sub_calendar_data_add" />
        <activity android:name=".PageReference"></activity>
    </application>

</manifest>